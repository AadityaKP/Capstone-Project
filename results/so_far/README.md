# Archived Results

Contents are organized by source:

- `legacy_root_exports/`: historical CSV result files that were previously saved in the repo root.
- `logs/`: experiment log files produced so far.
- `thesis_analysis_smoke/`: the complete smoke-run output pack, including traces, summaries, plots, and reports.

These files were moved here for consolidation and should be treated as a read-only archive snapshot.
