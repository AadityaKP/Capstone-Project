# Results Layout

- `so_far/`: archived outputs, CSVs, logs, and generated artifacts produced up to this point.
- `future_experiments/`: default destination for upcoming experiment runs.

This separation keeps historical results stable while new runs continue landing in a predictable location.
